This version should fix a bug originating from a Python library called zabbix-cli.
However, I recommend that you back up your database in case any problems arise.

```
mysqldump zabbix > zabbix-dump-$( date '+%Y-%m-%d_%H-%M-%S' ).sql -u root --password=''
```
Now using the default source repository `https://github.com/unioslo/zabbix-cli`.
