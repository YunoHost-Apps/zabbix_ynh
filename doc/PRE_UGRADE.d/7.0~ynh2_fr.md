Cette version devrait fixer un bug venant d'une librairie python zabbix-cli.
Cependant, je vous conseille de faire une sauvegarde de votre base de données au cas où un problème surviendrait.

```
mysqldump zabbix > zabbix-dump-$( date '+%Y-%m-%d_%H-%M-%S' ).sql -u root --password=''
```